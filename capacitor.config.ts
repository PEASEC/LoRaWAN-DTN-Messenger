import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.dtnmessenger',
  appName: 'dtnmessenger',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
